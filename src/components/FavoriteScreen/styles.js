import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  welcome: {
    fontSize: 20,
    textAlign: 'center',
    margin: 10,
  },
  feedText: {
    marginBottom: 10,
  },
  readPostBtn: {
    borderRadius: 0,
    marginLeft: 0,
    marginRight: 0,
    marginBottom: 0,
  },
  scrollToTopView: {
    width: 50,
    height: 50,
    position: 'absolute',
    bottom: 10,
    right: 0,
  },
})

export default styles
