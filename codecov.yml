codecov:
  max_report_age: off

comment: false

coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    project:
      default:
       threshold: 0.50
       if_not_found: success
    patch:
      default:
        enabled: no
        if_not_found: success
    changes:
      default:
        enabled: no
        if_not_found: success
