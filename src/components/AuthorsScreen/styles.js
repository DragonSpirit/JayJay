import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  authorInput: {
    fontSize: 20,
    textAlign: 'center',
    margin: 20,
    height: 40,
    width: '80%',
  },
  authorsContainer: {
    flex: 1,
    paddingTop: 30,
  },
})

export default styles
